#!/usr/bin/env bash
set -euf -o pipefail

TMPFILE=$(mktemp)
TERMINAL_ARGS=(
  -c "scratchpad"
  -e "${SHELL}"
  -l
  -c "${EDITOR} -c startinsert ${TMPFILE}"
)

"${TERMINAL}" "${TERMINAL_ARGS[@]}" && xclip -selection clipboard < "${TMPFILE}"
